<%= form_with(model: am) do |form| %>
  <% if am.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(am.errors.count, "error") %> prohibited this am from being saved:</h2>

      <ul>
        <% am.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :headline, style: "display: block" %>
    <%= form.text_field :headline %>
  </div>

  <div>
    <%= form.label :cinfo, style: "display: block" %>
    <%= form.text_field :cinfo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
