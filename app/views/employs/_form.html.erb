<%= form_with(model: employ) do |form| %>
  <% if employ.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employ.errors.count, "error") %> prohibited this employ from being saved:</h2>

      <ul>
        <% employ.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :cnic, style: "display: block" %>
    <%= form.text_field :cnic %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
